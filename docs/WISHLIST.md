# hit — wishlist

Every idea goes here, however wild. We refine them by using the tool.

**IDs:** `C-` core (Go binary, storage, search engine) · `S-` shell integration (pwsh, zsh) ·
`F-` user-facing features and UX · `T-` the finder's own layout, keys and chrome.
IDs are permanent. Don't renumber; mark items dropped instead.

`T-` was split out of `F-` once the finder grew enough to have its own questions (what a row
shows, where the preview sits, how you move around it). Finder items already filed under `F-`
stay there — IDs never move — so look in both when hunting for prior art.

**Milestones:** `M1` daily-drivable in pwsh · `M2` navigation + guards · `M3` zsh · `later` · `?` undecided

**Status:** `idea` → `planned` → `doing` → `done` · `dropped`

---

## Core

| ID | Item | MS | Status |
|---|---|---|---|
| C-001 | JSONL store: tolerant reader, record kinds `cmd`/`end`/`cd`/`del`, merge by id | M1 | done |
| C-002 | ULID ids; data/config dir resolution with env overrides | M1 | done |
| C-003 | Tombstone deletion (Del in the finder done; `hit rm <id>` still to do), hidden immediately | M1 | doing |
| C-004 | `hit compact`: lock, rewrite, catch late appends, atomic swap; many-shell stress test | M1 | planned |
| C-005 | Fuzzy match + ranking (match × recency × frequency × same-dir × success) | M1 | done |
| C-006 | Duplicate collapse with run count / last run | M1 | done |
| C-007 | `hit import psreadline` (backtick-continued multi-line entries) | M1 | planned |
| C-008 | `hit import mcfly` (SQLite, import-only dependency). mcfly redirects PSReadLine's history file to a temp file, so its DB holds the recent history | M1 | planned |
| C-009 | `hit import zsh` (EXTENDED_HISTORY) and `hit import atuin` | M3 | idea |
| C-010 | `hit init <shell>` emits the integration script matching the binary version (pwsh done) | M1 | doing |
| C-011 | Per-host/per-env history files (Windows, WSL, msys2, other machines); each appends only to its own, reader merges all | M3 | planned |
| C-012 | Resolve mapped drives ↔ UNC so `Z:\logs` and `\\srv\share\logs` find each other | M2 | idea |
| C-013 | Guard rules in `config.toml`, compiled into shell-native checks; reload on change | M2 | idea |
| C-014 | Secret redaction patterns (don't record / mask) applied before writing | M1 | idea |
| C-015 | Binary index cache beside the JSONL if load time ever becomes noticeable | later | idea |
| C-016 | `hit stats`: most-used commands, dirs, failure rates | later | idea |
| C-017 | Export / query-friendly view for `sql-cli` (flattened cmd+end rows as CSV/JSONL) | later | idea |
| C-018 | Test hooks: `HIT_DATA_DIR`/`HIT_CONFIG` isolation, injected clock + id source, `HIT_NOW` | M1 | done |
| C-019 | `hit search --filter … --print` (non-interactive, same ranking) | M1 | done |
| C-020 | Fuzz tests: reader tolerance + byte-exact round-trip of any command string | M1 | done |
| C-021 | Golden tests for search/ranking under `testdata/` with `-update` | M1 | done |
| C-022 | JSON Schema for records + result handoff in `schema/`; shared fixtures for all writers | M1 | doing |
| C-023 | CI: GitHub Actions windows + ubuntu: go vet/test/short fuzz, Pester, zsh tests, e2e smoke | M1 | doing |
| C-024 | Multi-process append + compact stress test | M1 | planned |
| C-025 | `hit import zoxide` (`zoxide query --list --score`) so directory ranking carries over | M2 | idea |
| C-026 | Learn from finder picks: record what was chosen (new record kind, schema first) and weight it in ranking, so the commands you actually reach for float up | ? | idea |
| C-027 | `hit path data\|history\|config`: print resolved paths for scripts and `hit doctor` | M1 | done |
| C-028 | Release workflow: tag `v*` cross-compiles windows/linux/darwin (amd64+arm64), publishes archives + SHA256SUMS, so hit installs where `proxy.golang.org` is blocked | M1 | done |
| C-029 | `HIT_TIMING` phase timings (DESIGN §15): elapsed ms for spawn, init, read, build, rank and first paint, shown in the finder and logged, so a slow Ctrl+R is attributed to a phase instead of guessed at. `--started-at` lets the binary measure its own process creation. Measured 2026-09-21: a day's history (70 commands / 380 lines) costs ~1 ms of an ~8 ms total, and 24.6k lines costs 37 ms — so recall latency is not history size | M1 | done |
| C-030 | Literal match mode beside the subsequence one. **Done:** a leading `'` matches literally, fzf-style; `^`/`$`/`!` and per-term splitting are not built. Matching is a pure subsequence, so `hit` matches `Get-History`, `Get-ChildItem` (h/i in "Child", t in "Item"), `Push-Location` and `$h = Invoke-RestMethod -Uri https://…` — 6 of 8 commands in a sample. Ranking floats the true hits to the top, which is why it has been bearable, but the tail fills the list once there is real history. Prior art worth copying rather than inventing: fzf's per-term syntax (`'exact`, `^prefix`, `suffix$`, `!negate`), which beats a global toggle because one query often wants both kinds of term. Smart-case already works this way and should stay | M1 | done |

## Shell

| ID | Item | MS | Status |
|---|---|---|---|
| S-001 | pwsh: record hook via `AddToHistoryHandler`, in-process append, no process spawn | M1 | done |
| S-002 | pwsh: prompt wrapper writes `end` (exit, duration) and `cd` records | M1 | done |
| S-003 | pwsh: Ctrl+R handler → `hit search` → replace buffer with the (multi-line) result. Must work in vi insert mode (owner uses `-EditMode vi`) | M1 | done |
| S-004 | pwsh: `Format-HitCommand` tidy via the PowerShell parser, with a token-equality check | M1 | done |
| S-005 | pwsh: store `$PWD.ProviderPath` (clean UNC), handle non-FS providers | M1 | done |
| S-006 | pwsh: leading-space = don't record | M1 | done |
| S-007 | pwsh: Enter guard (re-bind AcceptLine) for prod rules | M2 | idea |
| S-008 | pwsh: Alt+C directory finder → `Set-Location`; `j <terms>` jump | M2 | planned |
| S-009 | pwsh: Up/Down stepping backed by hit (prefix-filtered, multi-line aware) instead of PSReadLine's | ? | idea |
| S-010 | zsh: `zshaddhistory` / `precmd` / `chpwd` hooks, backgrounded writes | M3 | planned |
| S-011 | zsh: Ctrl+R / Alt+C widgets, `${(z)}` tidy formatter | M3 | planned |
| S-012 | zsh: antidote-installable plugin layout | M3 | planned |
| S-013 | zsh: `accept-line` guard wrapper | M3 | idea |
| S-014 | pwsh on Linux: verify everything works as on Windows | M3 | idea |
| S-015 | pwsh: Pester 5 suite; line-editor adapter (Get/Replace/Accept) so handlers run against a fake | M1 | doing |
| S-016 | pwsh: formatter corpus `tests/corpus/pwsh/` (expected output + token-equality) | M1 | done |
| S-017 | e2e smoke via pty/ConPTY: real shell + module, type multi-line, assert record; Ctrl+R recall | M1 | planned |
| S-018 | zsh: test harness (`zsh -f`, fake BUFFER) | M3 | planned |
| S-019 | `Invoke-HitSelfTest`: run formatter/round-trip checks over the owner's real history, locally only | M1 | idea |
| S-020 | C# `ICommandPredictor` plugin reading hit's history for PSReadLine predictions | later | idea |
| S-021 | `h` / `hit` command opens the finder as a no-key fallback | M1 | planned |
| S-023 | pwsh: `Join-HitCommand` + Alt+M toggle: split a one-liner at top-level parameters/pipes, or bring a continued command back onto one line for editing | M1 | done |
| S-026 | **Confirmed 2026-09-23: `spawn` measured 1.6 s on the work machine**, worse as the day goes on, sometimes near-free. S-029 found the cause: a first-ever launch of an unknown file costs ~3.5 s and ~58 ms thereafter, so this is a reputation cache being evicted and refilled — and our own release cadence refills it, because every new version is a new unknown file. The shell half of C-031. **Done:** `Enable-HitServer` / `Disable-HitServer` toggle it at a prompt with no restart, `$env:HIT_SERVER` turns it on at startup, the first recall of a session starts the server and pays the old price, and every failure falls back to spawning | M2 | done |
| S-029 | `scripts/measure-spawn.ps1`: tell apart "this binary is being inspected" from "every CreateProcess is hooked". **Answered 2026-09-23 on the work machine** (ms: first / min / median): `where.exe` 210 / 117 / 124, `hit version` 128 / 49 / 51, **a freshly-named copy of hit 3505 / 50 / 58**. So an unknown file costs ~3.5 s the first time it is ever launched and ~58 ms once known, while ordinary process creation is only mildly taxed (~50–124 ms, against 11–44 ms on an unmanaged box). The cost is per-file and cached, not per-CreateProcess | M1 | done |
| S-030 | Ask for the hit install **directory** to be allowlisted, not the file: every release is new content, so a hash- or file-based exclusion is spent the moment we ship. Better still if there is an internal CA — signing attaches reputation to the publisher and survives every future release. **Refused 2026-09-23: no exclusions outside system paths.** Signing is still worth asking about; C-031 was built instead | M1 | dropped |
| S-022 | `scripts/install.ps1`: build + install with version stamped; `-Clear` moves the current history into `backup\` (dogfooding: start clean each reinstall) | M1 | done |
| S-024 | `HIT_TIMING` phase timings in `Invoke-HitFinder` (DESIGN §15): temp-file create, buffer read, the run, read-back and redraw, plus `--started-at` so the binary can report process creation. Ctrl+R felt like 2.5–3 s on an AV-heavy work machine while the Go side is ~8 ms — this says which phase owns it | M1 | done |
| S-027 | Alt+M: let `Join-HitCommand` collapse statements by inserting `;`, so `& {` blocks and other multi-statement commands can come back onto one line. Today it refuses them ("it is more than one statement") because DESIGN §7 classes adding a separator as a rewrite. Needs the token-equality check extended to accept a `Semi` standing exactly where a `NewLine` was, and a corpus case per context where the two are *not* equivalent — this is the "spend real time on the parser" item | M1 | idea |
| S-028 | Alt+M round-trip corpus: for every command in `tests/corpus/pwsh/`, assert split-then-join returns the original text, so the two directions cannot drift apart | M1 | idea |
| S-031 | **A resident finder keeps running the binary it was started from**, so after an install the old code goes on answering Ctrl+R in every shell that already has one — which on a machine that ships often means testing a change against the version it replaced. Hit during the T-008 work: `alt+d` appeared to be missing until the shell was restarted. **Done:** `install.ps1` stops running servers unless `-KeepServers` is passed, which is safe because every failure on that path falls back to spawning. **Still open, and nicer — and most of it is already built:** the ping reply *already* carries the server's version (`Response.Version`), and `Test-HitServer` throws it away, checking only `pong`. So the handshake is: have the module compare that version with its own and treat a mismatch as "no server". It costs nothing on the hot path (the ping already happens), needs no install script to remember anything, and fixes the case the script cannot reach — a shell that was already open on another desktop | M2 | idea |
| S-032 | `Get-HitStatus`: the C-034 question from inside the shell, for this session only and with no process started. Adds what only the shell knows — whether server mode is on — and `Stale` when the server's version differs from the loaded module's (`$script:HitVersion`, now written by `hit init`). This reports the S-031 mismatch; acting on it in `Test-HitServer` is still S-031's open half | M2 | done |
| S-034 | Record each command's **shape** at capture: `simple`, `pipeline`, `chain` (`;`, `&&`, `\|\|` at top level) or `block` (`& { … }`). The shell parses every command anyway, so this is free, and it is a fact about the command like its exit code, not a category. Categories need it (DESIGN §17: a pipeline starting with git is `git`, a chain with git inside is not) and Go has no PowerShell parser. Schema first; old records fall back to a Go heuristic | M2 | idea |
| S-033 | **After an install, a shell that had already started a server never started another.** `Start-HitServer` tried once per session, `install.ps1` then stopped every server (S-031), and every Ctrl+R in that shell fell back to spawning — with `$env:HIT_SERVER` still `1` and server mode still on, which is what made it look odd. **Found at work 2026-09-25;** `Enable-HitServer` fixed it by hand, which confirmed the cause. **Done:** the flag became the time of the last attempt, and a recall that finds no server starts one if that was more than 30 s ago. Long enough for a new binary's ~3.5 s first launch (S-029) to finish listening; short enough that a killed server is back within a minute. The launch is a replaceable scriptblock so tests count starts instead of making them | M2 | done |
| S-025 | Stop using `[System.IO.Path]::GetTempFileName()` for the finder handoff: it creates a file in `%TEMP%`, which corporate AV scans on every Ctrl+R, and the Win32 call linear-probes for a free name as `%TEMP%` fills over a day. Use one fixed per-session path (or a pipe / stdout handoff) instead. Blocked on S-024 confirming it matters: read the `temp` phase | M1 | idea |

## Features / UX

| ID | Item | MS | Status |
|---|---|---|---|
| F-001 | Finder TUI: fuzzy list, multi-line preview pane with `⏎ +N` markers | M1 | done |
| F-002 | Del in finder = delete (with undo while open) | M1 | done |
| F-003 | Ctrl+E: edit selection in `$EDITOR`, return to prompt | M1 | planned |
| F-004 | Ctrl+R (again, inside the finder) scope cycle: dir → session → host → all | M1 | done |
| F-005 | Hide failed toggle (done); exit code / duration / cwd in preview (done); richer filters to come | M1 | doing |
| F-006 | Ctrl+F toggle "as typed" / "tidied"; decide format-on-demand vs pre-computed `fmt` | M1 | idea |
| F-007 | Syntax highlighting in the preview (pwsh and zsh) | later | idea |
| F-008 | Directory finder: frecency, never stats remote paths, explicit existence check on jump | M2 | planned |
| F-009 | Pivot: dir → commands run there; command → jump to its dir | M2 | idea |
| F-010 | Prod guards: warn / confirm-word / block; red marker on matching entries in the finder. Likely built as a property of a C-036 category rather than its own rule list | M2 | idea |
| F-011 | Convert to splatting as a tidy mode | later | idea |
| F-012 | Bulk delete: select many / delete all matching the current filter (junk-paste cleanup) | M1 | idea |
| F-013 | Pin / favourite commands and directories; pinned float to top | later | idea |
| F-014 | Tags or notes on a command ("reindex logs-2026") that are searchable | later | idea |
| F-015 | Find files under previously visited dirs (search inside where you've been) | later | idea |
| F-016 | Time filter ("last week", "Monday") in the finder. Folded into T-002 | later | dropped |
| F-017 | Guard conditions on time/day (extra-strict before 10:00 on a Monday) | later | idea |
| F-018 | Keymap from `config.toml`, no hard-coded keys | M1 | planned |
| F-019 | `hit doctor`: detect Zellij/tmux/WT/WSL/msys2, report key clashes and setup problems | M2 | idea |
| F-026 | Finder rows show the command flattened onto one line, not just its first line: a block starting `& {` rendered as `& {` and nothing else, so every command of that shape looked identical. Match highlighting maps back to the original offsets, so matches on later lines now show too | M1 | done |
| F-027 | Finder ignores Alt chords instead of typing them into the filter: Alt+M in the finder was silently narrowing the list to whatever matched "m", so the key looked dead and quietly did the wrong thing | M1 | done |
| F-020 | Inline vs full-screen mode (still full-screen); layout adapts to pane size (done) | M1 | doing |
| F-021 | Shell-family filter by default; WSL/msys2/Windows path translation in dir finder | M3 | idea |
| F-022 | Leader key inside the finder, only if clashes pile up | ? | idea |
| F-023 | Drop-in `cd` like `zoxide --cmd cd`: a real path → Set-Location, otherwise jump to the best frecency match; `cdi` interactive. Replaces zoxide (and ZLocation). **Owner's two complaints about zoxide, 2026-09-24, both daily:** (1) with repos `gdt-platform` and `gdt-platform-launch`, `cd gdt-platform` goes to `-launch` every time, because frecency outranks an exact name — see C-035; (2) it is useless on Windows network shares, which are much of the working day. DESIGN §8 already says never stat on the query path and never silently prune; C-012 covers mapped drives ↔ UNC. Pin down the exact share failures from use before building. Directories and commands should share one finder (T-011), with bookmarks (F-028) | M2 | idea |
| F-029 | **Guards on a command in a context** (DESIGN §9.1). The owner's biggest fear: `Set-ElasticEnv PROD`, then a command that mutates the stack, sent without remembering where the shell points. A guard gains `when` (a live environment variable, read in-process at Enter) and `unless` (so Elastic's read-only POSTs, `_search`/`_count`/`_msearch`, pass silently: false alarms train you to confirm without reading). The owner's own functions are judged by PowerShell verb (`Get-` reads, `Set-`/`Remove-`/`New-`/… mutate). The finder marks a row red if it would trip a guard in the shell's *current* state. Limits, stated: a guard sees only the typed line, never inside a script. Builds on C-013, S-007, F-010 | M2 | idea |
| F-028 | Directory bookmarks: pin favourites so they are always offered and ranked first, independent of frecency, and survive not being visited for a while. Schema first — a new record kind, so the stored history is never rewritten — and shared with the directory finder (T-011) | M2 | idea |
| F-024 | Retrospective lock: mark a command in the finder as guarded, so sending it (or a match) again asks for confirmation first. Per-command guards built on F-010/C-013 | M2 | idea |
| F-025 | navi-style expansion: a short alias expands in place at the prompt into a stored (multi-line) command, with placeholders to fill in. Aliases in config or as tagged history entries (F-014) | later | idea |

## TUI (the finder)

| ID | Item | MS | Status |
|---|---|---|---|
| T-001 | Show each command's time in the list itself, not only in the preview, so a run can be placed against the working day without moving the cursor onto it. **Done:** fixed six-wide column on the left, dimmed; clock for today, `02 Jan` otherwise, blank when the record has no timestamp; dropped below a 40-column pane rather than squeezing the command | M1 | done |
| T-002 | Time filter in the finder: today / this week / since a date. Pairs with T-001 — once the time is visible, wanting to narrow by it follows. Decide whether it is a cycling toggle like `^x`, a scope-style cycle, or typed into the query (`>today`). Now that T-001 shows the time, this is the natural next step. Prior art in `mless`: `Pane.parseTimeInput` takes `15:04`, `15:04:05` and full dates, and anchors a bare time to the document's day with a midnight-rollover guard. hit would anchor to *now* instead and wants relative forms (`2h`, `today`) that mless has no use for — copy the shape, not the file. **Owner's framing 2026-09-24, semantics not yet settled:** no date picker, no parsing cleverness — crude whole days. The motivating case is "the variant I want was run *yesterday*", among thousands of commands. So the unit is a slice of days: an **anchor** day (today by default) and a **width** going back from it. `today` is anchor 0, width 1; "the last three days" is anchor 0, width 3; "yesterday only" is anchor −1, width 1. Probably two keys, one to move the anchor a day back or forward and one to widen or narrow, with the slice named permanently in the header like scope and sort (T-009) and an empty list saying which key widens it (T-008). Undecided: whether moving the anchor keeps the width; whether days break at midnight or at a configurable hour for late sessions; how it sits alongside scope. Supersedes F-016 | M1 | idea |
| T-003 | Preview placement: expand the selected row **inline** rather than in a fixed pane at the bottom. With a full list the eye has to travel to the foot of the screen and back to read a command it is already sitting on. Undecided — inline costs a stable layout (rows move as you scroll) and complicates the `⏎ +N` marker. Prototype both and pick by using them (F-020 covers the inline/full-screen frame; this is about where the *command body* goes) | M1 | idea |
| T-004 | Vim-style movement in the finder: `gg` / `G` for top and bottom, and whatever else earns its place (`d`/`u` half-page?). The owner runs PSReadLine in vi edit mode, so the muscle memory is already there. Blocked on T-006: without a mode, every one of these keys is filter text. Keymap should match `mless` key for key where the two overlap — the muscle memory is the point | M1 | idea |
| T-005 | The finder's half of C-030. **Done:** the header names `literal` next to the scope, so the mode is not left to be inferred from a quote that is easy to miss. Still open: a whole-query toggle for when quoting every term gets tiresome, once per-term syntax exists | M1 | doing |
| T-006 | A mode split in the finder, so navigation keys can exist at all. Today every printable key is filter text, which is why T-004 has nowhere to put `gg`. `mless` gets single-letter motions for free because nothing is accepting text in its normal mode — hit's query box is always live, so it has to choose. Options: Esc switches filter → normal (Esc again, or `q`, then cancels) which fits vi edit mode at the prompt and is the only one that scales to counts and marks; or the Alt namespace, free since F-027, which needs no mode but is not vim and stops at a handful of chords. Take `mless`'s shape — a `Mode` field dispatched at the top of `Update`, plus a `countPrefix` accumulated from digits and consumed by the next command — but copy the pattern, not the code, and do not copy its trick of overloading `countPrefix = -1` as a "waiting for a mark" sentinel | M1 | idea |
| T-008 | **Alt+D: this folder only ⇄ back.** The directory filter existed as one stop on the `^r` scope cycle, and that served it badly: from `all`, one press lands on the *narrowest* scope, so in a directory with nothing recorded the list empties — and getting out took three more presses through `session` and `host`, which in that directory were empty too. **Reported from daily use 2026-09-24: "I press ctrl-r within the TUI and it shows no rows, and ctrl-r again doesn't take me back to all, I then have to kill it."** The cycle was working exactly as designed; the design was wrong. **Done:** the cycle now narrows one step at a time (`all → host → session → dir`, pinned by a test in `internal/search`) so each press shrinks the list and the first lands somewhere with rows; and `alt+d` is one key in and the same key out, restoring the scope it found. The header names the scope permanently and names the *folder* when filtering on one, and an empty list says why it is empty and which key widens it | M1 | done |
| T-009 | **Alt+S: sort rank ⇄ recent.** The finder's half of C-032. **Done:** the header names the order permanently, default or not, because an indicator that only appears when it is on teaches nothing about the key that turns it off | M1 | done |
| T-010 | **The selected row was highlighted for two characters.** A row is drawn from several styles — the time column, the matched runes, the `⏎ +N` marker — each ending in a full SGR reset, so wrapping the finished line in a "selected" style painted the bar only as far as the first nested style: the `▸ ` prefix, and nothing else. Measured: 10 painted cells of 60. Every style involved was correct on its own, which is why it survived review. **Reported from daily use 2026-09-24: moving up and down the list, the current row isn't clearly shown.** **Done:** the selected row is drawn from its own set of styles, each already carrying the highlight, padded to the full pane width so the selection is a band across the list; the test walks the rendered SGR escapes and asserts every visible cell is painted, so this cannot regress quietly | M1 | done |
| T-011 | **One finder for directories and commands**, not two tools. The same TUI, keys and scope/sort header, with a filter for what the rows are: commands, directories, or both. DESIGN §8 already plans the pivot both ways (from a directory, the commands run there; from a command, the directory it ran in). The TUI half of F-023, with bookmarks (F-028) shown first. The pipe (C-031) makes it cheap enough to be a reflex: recall is now under 1 ms once the server is warm, per the owner 2026-09-24. **Owner 2026-09-25: the zoxide replacement is a bucket, not a tool.** Navigation is a subset of the one history, gathered under a single umbrella (say `cd`), and the directory explorer / `z` jump is simply the finder looking only at that bucket. Most of the bucket exists already: the prompt hook writes a `cd` record on every directory change, however it happened. So `cd` is a C-036 category with a known name, and the one exception to "ship no categories": it comes predefined as `kind = "cd"` because F-023 and `z` depend on it, while staying overridable in config like any other. Its rows differ in shape from command rows: one per directory, not per visit, ranked by directory frecency with C-035's exact-name rule on top, and the other categories (share, dev) apply to them too, so "network shares I have been to" is two layers of the T-013 stack | M2 | idea |
| T-012 | **A subtle category marker per row** (C-036), not a Christmas tree. Colour comes from the category's config, and most categories should get a quiet one; red is kept for *danger*, which should stand out. Where: the time column's colour (T-001) or a one-cell gutter mark, not a row background, because the background already means "selected" (T-010) and the two would fight. An uncategorised row looks exactly as it does today. Must stay legible with colour off (`NO_COLOR`, a monochrome terminal): the gutter mark could be a letter | M2 | idea |
| T-013 | **Filters as a stack.** Scope, this folder (T-008), literal (T-005), the time slice (T-002), category (C-036) and the row kind (T-011) each narrow the list, and there will be more. Stop treating each as its own toggle with its own undo: every filter pushed is one layer, the header shows the layers left to right as small chips (it already names scope and sort permanently, T-009), and one key pops the top layer. Backspace on an empty query is the natural pop key, since the query is conceptually the top layer anyway. Sort is an ordering, not a filter, so it stays outside the stack. The empty-list message (T-008) names the top layer as the one to pop. Picking a category: cycle with a key, or a small list when there are more than a few | M2 | idea |
| T-007 | Do **not** lift `mless`'s `Viewport`. It is the obvious-looking candidate and it is the wrong shape: it scrolls a window over a document and its highlighted line is optional (`-1` by default), whereas the finder moves a selection that always exists and lets the window follow. `clampCursor` is already the right twenty lines for that job. Recorded so the idea does not get re-proposed every few months | — | dropped |

## Core (continued)

| ID | Item | MS | Status |
|---|---|---|---|
| C-032 | **Sort order as a mode: `rank` ⇄ `recent`.** Ranking answers "what do I most likely want next" and is right nearly always; it is wrong when you are walking back through the last few things you ran, because frecency actively fights that — a command run 200 times outranks the one run once, five minutes ago. So this is a second question, not a worse formula, and it gets a mode rather than a tuning change. **Done:** `search.Query.Sort`, `--sort rank\|recent` on `hit search` (so `--print` reaches it too), a matching field on the `hit serve` request so the cold and warm routes can't drift, and Alt+S in the finder (T-009). Matching is untouched: the same commands match and only the order changes, which the tests assert directly. Duplicate collapse, scope and the failed-exit filter all still apply, and an undated record sorts last rather than first | M1 | done |
| C-033 | **The resident finder outlived its shell, because a process id is not an identity.** `watchParent` asked "is a process with id N running" on a timer; Windows recycles ids briskly, so the shell exited, its id was handed to something else, and the watch kept answering yes about a stranger. **Found from daily use 2026-09-24:** the owner killed a shell, `hit serve --parent 23740` was still resident, and pid 23740 had become a `dotnet` process. **Done:** the parent is pinned with an open `SYNCHRONIZE` handle taken at startup, which asks about that exact process *and* stops Windows reusing the id while the handle is held — the fix and the race close together. Unix has no equivalent; the id is kept with the caveat recorded in the source, and a pidfd (Linux 5.3+) is the proper fix when the server ships beyond Windows | M2 | done |
| C-034 | **`hit status`: what is resident, and whose is it.** Both server bugs so far (C-033, S-031) were found by noticing a process and being unable to say what it was — and on a managed machine that question may come from someone else. **Done 2026-09-24:** the ping reply carries a `server` block (pid, parent, parent alive per the pinned handle, endpoint, exe, started, idle limit, finder requests and last use — pings do not count as use). `hit status` walks `\\.\pipe\hit-*` and asks every endpoint, not just this shell's, since an orphan or a pre-install server is exactly what a per-session question misses; it marks the one whose parent is the calling shell, says in words when a server is on another version or its shell has gone, bounds each ask at 1 s so a server busy drawing in another shell is reported rather than waited on, and has `--json`. Shell half is S-032 | M2 | done |
| C-035 | **An exact name beats frecency** in directory jumps. If a recorded directory's last segment equals the query exactly (case-insensitive on Windows), it wins however weak its rank; frecency only orders several exact matches, or applies when there is none. From daily use with zoxide: `gdt-platform` beside `gdt-platform-launch`, where `cd gdt-platform` lands in `-launch` every time. Needs a golden test with exactly that pair before any ranking code (DESIGN §13.3). Likely wanted for multi-term queries too (the last term matching the last segment exactly) — decide when building | M2 | idea |
| C-037 | **Record which environment a command ran against.** Config names a few context variables (e.g. whatever `Set-ElasticEnv` sets); their values are stored with each command, so history can answer "what did I run against prod last Tuesday", and the finder can filter by it (a T-013 layer) or show it in the preview. Names come from config, so nothing is captured unless asked for, and only values the owner has chosen as non-secret. Pairs with F-029: the guard reads the environment now, this remembers it then | M2 | idea |
| C-036 | **Categories, defined in config, never in code.** Owner 2026-09-25: broad buckets, not many of them, and not baked in or the tool becomes one person's taste. Examples: *network shares* (`cd \\something.pte\logs`, anything under a UNC root), *dev* (anything under `~\dev`, i.e. the repos), *danger* (`Restart-Computer` and the like). A rule in `config.toml` matches on any of: command text (regex, as the §9 guards already do), the directory it ran in (prefix or glob, so a whole tree is one line), and the record kind (`cmd` or `cd`). **Evaluated when history is read, never stored**, so the stored command is never touched, editing a rule reclassifies all past history at once, and deleting the config loses nothing. A row may fall in several categories: the filter matches any of them, and the marker shows the first in config order, so order is priority. **Guards become a property of a category** (`guard = "confirm"` on *danger*) instead of a parallel rule list: one match language, and a guarded command is marked in the finder for free (F-010). Ship no categories, only a commented example. Cost: classify each distinct command once per read, not per keystroke; the server can keep compiled rules and reload when the file's mtime changes (C-013). Open: whether a query term can name a category (`@share`) as well as a key. **Owner's working set 2026-09-26, recorded in DESIGN §17:** `git`, `navigation`, `content` (web fetches and archives), `environment` (env vars and credentials) and `devops` (a placeholder that grows by hand), matched on a command's first word so chains stay unlabelled | M2 | idea |
| C-031 | `hit serve`: a warm process per shell session, so the spawn is paid once instead of on every recall. **Not a service** — no registration, no admin, no autostart, no persistence: a child of the user's own shell that dies with it, which is what `gopls`, `VBCSCompiler` and MSBuild's node reuse all are. The owner was questioned over installing a Windows service, so the distinction is the whole point and should be stated wherever this is documented. Idle timeout as a backstop against orphans, in the dotnet style. **The crux is the console, not the IPC:** the TUI draws by inheriting the real console, so the warm process must hold it while idle and take it only for the length of a request, entering and leaving raw mode and the alternate screen once per request rather than once per process — prove terminal state survives a few hundred cycles before building anything on it. **Done** (DESIGN §16). S-030 was refused — the site allows no exclusions outside system paths — so this is the fix. Opt-in, one request at a time, cold spawn as the fallback for every failure, 30 min idle default. Not a service: a child of the shell, on an endpoint scoped to the user's SID, gone when the shell exits | M2 | done |
